class DS8RGlobals:
    ds8r_mode = 2
    ds8r_polarity = 1
    ds8r_source = 1
    ds8r_demand = 40
    # positive part of pulse
    ds8r_pulse_width = 500
    # the interval between the positive and negative part of the biphasic pulse set up the default max stimulation
    ds8r_dwell = 1
    # recovery phase ratio - a percentage, 100% means stim and recovery is the same duration and amplitude
    ds8r_recovery = 100
    ds8r_enabled = 1
